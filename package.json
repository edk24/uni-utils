{
  "name": "uni-utils",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@dcloudio/types": "^3.4.14",
    "lerna": "^8.1.9"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "test": "lerna run test"
  }
}
